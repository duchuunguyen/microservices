server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        encrypt.enabled: false
        git:
          uri: file://${user.home}/it/code/microservices/configuration/resources/{application}
